import { ApplicationCommandType } from 'discord.js';

/**
 * Context menu generatePayout command
 */
export const generatePayout: any = {
    name: 'Generate payout',
    type: ApplicationCommandType.Message,
}
